{
  "name": "kladochnik",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server.js",
    "build": "cd client && bun run build",
    "start": "bun run server.js",
    "test": "bun test test/api.test.js"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "@hono/node-server": "^1.8.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
